import { motion } from "framer-motion";
import styled from "styled-components";

export const SubTitle = styled(motion.h2)`
  color: #a5a5a5;
  font-family: "Circular Std Medium", "Inter", "Robot", sans-serif;
  font-weight: normal;
  font-size: 1.2rem;
`;
