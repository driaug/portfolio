import { motion } from "framer-motion";
import styled from "styled-components";

export const MainTitle = styled(motion.h1)`
  font-family: "Circular Std Bold", "Inter", "Roboto", sans-serif;
  font-size: 3.5rem;
  padding-right: 10px;
`;
